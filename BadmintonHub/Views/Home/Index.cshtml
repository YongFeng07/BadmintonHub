@model BadmintonHub.ViewModels.HomeViewModel
@{
    ViewData["Title"] = "Home";
}

<div class="bh-hero">
    <div class="row align-items-center">
        <div class="col-lg-8">
            <h1 class="display-5 mb-3">Book Your Court.<br /><span class="bh-hero-accent">Play Your Game.</span></h1>
            <p class="lead mb-4">
                @(Model.Facility?.Name ?? "BadmintonHub") — six professional indoor courts with easy online booking.
                Pick a date, choose your time, and step on court.
            </p>
            <a asp-controller="Courts" asp-action="Index" class="btn btn-bh-amber btn-lg px-4 me-2">Browse Courts</a>
            @if (User.Identity?.IsAuthenticated == true)
            {
                <a asp-controller="Courts" asp-action="Index" class="btn btn-outline-light btn-lg px-4">Check Availability</a>
            }
            else
            {
                <a asp-controller="Account" asp-action="Login" class="btn btn-outline-light btn-lg px-4">Login to Book</a>
            }
        </div>
        <div class="col-lg-4 d-none d-lg-block text-end">
            <span style="font-size: 7rem; opacity: .9;">🏸</span>
        </div>
    </div>
    <div class="row g-3 mt-4">
        <div class="col-6 col-md-3">
            <div class="bh-stat"><span class="number">@Model.CourtCount</span><span class="label">Indoor Courts</span></div>
        </div>
        <div class="col-6 col-md-3">
            <div class="bh-stat"><span class="number">@Model.MemberCount</span><span class="label">Members</span></div>
        </div>
        <div class="col-6 col-md-3">
            <div class="bh-stat"><span class="number">@Model.TodayReservations</span><span class="label">Today's Bookings</span></div>
        </div>
        <div class="col-6 col-md-3">
            <div class="bh-stat"><span class="number">8–23</span><span class="label">Open Daily (Hrs)</span></div>
        </div>
    </div>
</div>

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2 class="bh-page-title mb-0">Our Courts</h2>
    <a asp-controller="Courts" asp-action="Index" class="btn btn-bh btn-sm">View All →</a>
</div>

<div class="row g-4">
    @foreach (var court in Model.FeaturedCourts)
    {
        var primaryPhoto = court.Photos.OrderBy(p => p.DisplayOrder).FirstOrDefault();
        <div class="col-md-6 col-lg-4">
            <div class="card bh-card bh-court-card h-100">
                @if (primaryPhoto != null)
                {
                    <img src="@primaryPhoto.FilePath" class="card-img-top" alt="Court @court.CourtNumber" />
                }
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start mb-2">
                        <h5 class="card-title mb-0">Court @court.CourtNumber</h5>
                        <span class="badge badge-status badge-@court.CourtType.ToString().ToLower()" style="background:#eef3f8;color:#3c5670;">@court.CourtType</span>
                    </div>
                    <p class="text-muted small mb-3">
                        @(string.IsNullOrEmpty(court.Description) ? "Indoor badminton court." : court.Description.Length > 90 ? court.Description[..90] + "…" : court.Description)
                    </p>
                    <div class="d-flex justify-content-between align-items-center">
                        <span class="fw-bold text-bh-green">RM @court.HourlyRate.ToString("0.00") <span class="text-muted fw-normal">/ hour</span></span>
                        <a asp-controller="Courts" asp-action="Details" asp-route-id="@court.Id" class="btn btn-bh btn-sm">View Court</a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@if (Model.Facility != null)
{
    <div class="bh-form-section mt-5 d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
        <div>
            <h4 class="mb-1">📍 @Model.Facility.Name</h4>
            <p class="mb-0 text-muted">@Model.Facility.Address · Open daily @Model.Facility.OpeningTime.ToString(@"hh\:mm") – @Model.Facility.ClosingTime.ToString(@"hh\:mm")</p>
        </div>
        <a asp-controller="Facility" asp-action="Index" class="btn btn-outline-success flex-shrink-0">About the Facility</a>
    </div>
}
